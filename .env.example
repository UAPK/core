# UAPK Environment Variables
# Copy this file to .env and fill in your values
# Never commit .env to version control!

# ==================== Platform Paths (Phase 0) ====================

# Code directory (repo location)
# UAPK_CODE_DIR=/opt/uapk

# Data directory (instances, CAS, DB, runtime state)
# UAPK_DATA_DIR=/var/lib/uapk

# Log directory
# UAPK_LOG_DIR=/var/log/uapk

# ==================== Required Secrets (M1.5) ====================

# JWT Secret Key (for signing access tokens)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
UAPK_JWT_SECRET_KEY=your-jwt-secret-key-here-change-me

# Fernet Key (for encrypting stored secrets in database)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
UAPK_FERNET_KEY=your-fernet-key-here-change-me

# ==================== Optional Secrets ====================

# Ed25519 Private Key (for signing audit events and override tokens)
# If not set, auto-generated and stored in runtime/keys/ (development mode)
# For production, generate with ssh-keygen or openssl and set here
# UAPK_ED25519_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
# ...PEM content...
# -----END PRIVATE KEY-----"

# ==================== Connector Secrets (M1.3) ====================

# OpenAI API Key (if using real LLM integration)
# UAPK_SECRET_OPENAI_API_KEY=sk-...

# Stripe API Key (if using real payment processing)
# UAPK_SECRET_STRIPE_API_KEY=sk_test_...

# SendGrid API Key (if using real email sending)
# UAPK_SECRET_SENDGRID_API_KEY=SG....

# Add more connector secrets as needed:
# UAPK_SECRET_<NAME_UPPER>=<value>

# ==================== Database Configuration ====================

# Database URL (SQLite default, PostgreSQL recommended for production)
# DATABASE_URL=sqlite:///./runtime/opspilotos.db
# DATABASE_URL=postgresql://user:pass@localhost/opspilotos

# ==================== Server Configuration ====================

# Server host and port
# HOST=0.0.0.0
# PORT=8000

# Environment (development, production)
# ENVIRONMENT=development

# ==================== Blockchain Configuration (Phase 4) ====================

# Chain RPC URL (for local Anvil chain)
UAPK_CHAIN_RPC=http://127.0.0.1:8545

# Chain private key (for minting NFTs - DEV KEY ONLY, DO NOT USE IN PRODUCTION)
# This is Anvil's default dev key #0
UAPK_CHAIN_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# NFT contract address (optional - will be deployed if not set)
# UAPK_NFT_CONTRACT_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3

# Chain ID (31337 for Anvil local, 1 for mainnet)
UAPK_CHAIN_ID=31337

# ==================== Notes ====================

# - All UAPK_* variables are specific to UAPK Gateway
# - Connector secrets follow pattern: UAPK_SECRET_<NAME>
# - Required secrets (JWT, Fernet) must be set or app will fail to start
# - Optional secrets will use fallback/mock values if not set
